<UserControl x:Class="Client.Pages.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Margin="4" Content="Email:"/>
        <Label Grid.Row="1" Grid.Column="0"  Margin="4" Content="Password:"/>
        <TextBox x:Name="emailLogin" Grid.Column="1" Grid.Row="0" Margin="4" />
        <PasswordBox x:Name="passwordLogin" Grid.Column="1" Grid.Row="1" Margin="4" />
        <Button x:Name="loginButton" Grid.Column="1" Click="LoginButton_OnClick"  Grid.Row="2" HorizontalAlignment="Right" 
            MinWidth="90" MinHeight="30" Margin="4,4,4,4" Content="Login"  />
     
    </Grid>
</UserControl>
